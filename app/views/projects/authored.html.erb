<% if @projects.present? %>
    <h1>Here are the projects you authored</h1>
<ul>
    <% @projects.each |project| %>
        <li><%= link_to project.title, project_path(project) %></li>
    <% end %>
</ul>
<% else %>
    <h1>You don't have any projects yet</h1>
<% end %>
